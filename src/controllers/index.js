const userController = require('./user.controller');
const categorieController = require('./categorie.controller');
const postController = require('./post.controle');

module.exports = {
    userController,
    categorieController,
    postController,
};